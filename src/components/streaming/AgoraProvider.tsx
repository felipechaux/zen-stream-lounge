
// Placeholder for Ant Media Server integration
// Remove this file or replace with Ant Media context/provider as needed
export default function EmptyProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
